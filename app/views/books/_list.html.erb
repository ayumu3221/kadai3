<h1>Books</h1>
<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
  </tr>
    <tbody>
      <% books.each do |book| %>
      <tr>
        <td><span><%= image_tag book.user.get_profile_image(100,100) %></span></td>
        <td><span><%= book.user.name %></span></td>
        <td><span><%= link_to book.title, book_path(book.id) %></span></td>
        <td><span><%= book.body %></span></td>
      </tr>
      <% end %>
    </tbody>
</table>
